schema = "0.1"

[project]
name = "must-sample"
version = "0.1.0"
license = "MPL-2.0-or-later"
author = "Hyperpolymath"

[requirements]
must_have = [
    "README.adoc",
]

must_not_have = [
    "Makefile",
]

[requirements.content]
"README.adoc" = ["Sample Project"]
