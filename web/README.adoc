// SPDX-License-Identifier: PMPL-1.0-or-later
= web/
Static web UI (HTML/CSS/JS, no build step): `index.html`, `styles.css`, `app.js`, and a sample `export.json` for offline exploration of graph data.
