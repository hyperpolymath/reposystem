// Generated by ReScript, PLEASE EDIT WITH CARE

import * as List from "rescript/lib/es6/list.js";
import * as $$String from "rescript/lib/es6/string.js";
import * as Caml_obj from "rescript/lib/es6/caml_obj.js";
import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as PervasivesU from "rescript/lib/es6/pervasivesU.js";
import * as Webapi__Dom__Element from "rescript-webapi/src/Webapi/Dom/Webapi__Dom__Element.res.js";
import * as Webapi__Dom__HtmlElement from "rescript-webapi/src/Webapi/Dom/Webapi__Dom__HtmlElement.res.js";

var noNode = {
  TAG: "CommentNode",
  _0: ""
};

function comment(s) {
  return {
          TAG: "CommentNode",
          _0: s
        };
}

function text(s) {
  return {
          TAG: "Text",
          _0: s
        };
}

function fullnode(namespace, tagName, key, unique, props, vdoms) {
  return {
          TAG: "Node",
          _0: namespace,
          _1: tagName,
          _2: key,
          _3: unique,
          _4: props,
          _5: vdoms
        };
}

function node(namespaceOpt, tagName, keyOpt, uniqueOpt, props, vdoms) {
  var namespace = namespaceOpt !== undefined ? namespaceOpt : "";
  var key = keyOpt !== undefined ? keyOpt : "";
  var unique = uniqueOpt !== undefined ? uniqueOpt : "";
  return fullnode(namespace, tagName, key, unique, props, vdoms);
}

function lazyGen(key, fn) {
  return {
          TAG: "LazyGen",
          _0: key,
          _1: fn,
          _2: {
            contents: noNode
          }
        };
}

function prop(propName, value) {
  return {
          TAG: "RawProp",
          _0: propName,
          _1: value
        };
}

function onCB(key, eventName, cb) {
  return {
          TAG: "Event",
          _0: eventName,
          _1: {
            TAG: "EventHandlerCallback",
            _0: key,
            _1: cb
          },
          _2: {
            contents: undefined
          }
        };
}

function onMsg(name, msg) {
  return {
          TAG: "Event",
          _0: name,
          _1: {
            TAG: "EventHandlerMsg",
            _0: msg
          },
          _2: {
            contents: undefined
          }
        };
}

function attribute(namespace, key, value) {
  return {
          TAG: "Attribute",
          _0: namespace,
          _1: key,
          _2: value
        };
}

function data(key, value) {
  return {
          TAG: "Data",
          _0: key,
          _1: value
        };
}

function style(key, value) {
  return {
          TAG: "Style",
          _0: {
            hd: [
              key,
              value
            ],
            tl: /* [] */0
          }
        };
}

function styles(s) {
  return {
          TAG: "Style",
          _0: s
        };
}

function createElementNsOptional(namespace, tagName) {
  var $$document$1 = document;
  if (namespace === "") {
    return $$document$1.createElement(tagName);
  } else {
    return $$document$1.createElementNS(namespace, tagName);
  }
}

function nodeAt(index, nodes) {
  return Belt_Option.getExn(Caml_option.nullable_to_opt(nodes.item(index)));
}

function renderToHtmlString(_x) {
  while(true) {
    var x = _x;
    switch (x.TAG) {
      case "CommentNode" :
          return "<!-- " + (x._0 + " -->");
      case "Text" :
          return x._0;
      case "Node" :
          var tagName = x._1;
          var namespace = x._0;
          return $$String.concat("", {
                      hd: "<",
                      tl: {
                        hd: namespace,
                        tl: {
                          hd: namespace === "" ? "" : ":",
                          tl: {
                            hd: tagName,
                            tl: {
                              hd: $$String.concat("", List.map((function (p) {
                                          if (typeof p !== "object") {
                                            return "";
                                          }
                                          switch (p.TAG) {
                                            case "RawProp" :
                                                return $$String.concat("", {
                                                            hd: " ",
                                                            tl: {
                                                              hd: p._0,
                                                              tl: {
                                                                hd: "=\"",
                                                                tl: {
                                                                  hd: p._1,
                                                                  tl: {
                                                                    hd: "\"",
                                                                    tl: /* [] */0
                                                                  }
                                                                }
                                                              }
                                                            }
                                                          });
                                            case "Attribute" :
                                                return $$String.concat("", {
                                                            hd: " ",
                                                            tl: {
                                                              hd: p._1,
                                                              tl: {
                                                                hd: "=\"",
                                                                tl: {
                                                                  hd: p._2,
                                                                  tl: {
                                                                    hd: "\"",
                                                                    tl: /* [] */0
                                                                  }
                                                                }
                                                              }
                                                            }
                                                          });
                                            case "Data" :
                                                return $$String.concat("", {
                                                            hd: " data-",
                                                            tl: {
                                                              hd: p._0,
                                                              tl: {
                                                                hd: "=\"",
                                                                tl: {
                                                                  hd: p._1,
                                                                  tl: {
                                                                    hd: "\"",
                                                                    tl: /* [] */0
                                                                  }
                                                                }
                                                              }
                                                            }
                                                          });
                                            case "Event" :
                                                return "";
                                            case "Style" :
                                                return $$String.concat("", {
                                                            hd: " style=\"",
                                                            tl: {
                                                              hd: $$String.concat(";", List.map((function (param) {
                                                                          return $$String.concat("", {
                                                                                      hd: param[0],
                                                                                      tl: {
                                                                                        hd: ":",
                                                                                        tl: {
                                                                                          hd: param[1],
                                                                                          tl: {
                                                                                            hd: ";",
                                                                                            tl: /* [] */0
                                                                                          }
                                                                                        }
                                                                                      }
                                                                                    });
                                                                        }), p._0)),
                                                              tl: {
                                                                hd: "\"",
                                                                tl: /* [] */0
                                                              }
                                                            }
                                                          });
                                            
                                          }
                                        }), x._4)),
                              tl: {
                                hd: ">",
                                tl: {
                                  hd: $$String.concat("", List.map((function (v) {
                                              return renderToHtmlString(v);
                                            }), x._5)),
                                  tl: {
                                    hd: "</",
                                    tl: {
                                      hd: tagName,
                                      tl: {
                                        hd: ">",
                                        tl: /* [] */0
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    });
      case "LazyGen" :
          var vdom = x._1();
          _x = vdom;
          continue ;
      case "Tagger" :
          _x = x._1;
          continue ;
      
    }
  };
}

function emptyEventHandler(_ev) {
  
}

function emptyEventCB(_ev) {
  
}

function eventHandler(callbacks, cb) {
  return function (ev) {
    var msg = cb.contents(ev);
    if (msg !== undefined) {
      return callbacks.contents.enqueue(Caml_option.valFromOption(msg));
    }
    
  };
}

function eventHandlerGetCB(handlerType, ev) {
  if (handlerType.TAG === "EventHandlerCallback") {
    return handlerType._1(ev);
  } else {
    return Caml_option.some(handlerType._0);
  }
}

function compareEventHandlerTypes(left, right) {
  if (right.TAG === "EventHandlerCallback") {
    if (left.TAG === "EventHandlerCallback") {
      return right._0 === left._0;
    } else {
      return false;
    }
  } else if (left.TAG === "EventHandlerCallback" || !Caml_obj.equal(right._0, left._0)) {
    return false;
  } else {
    return true;
  }
}

function eventHandlerRegister(callbacks, elem, name, handlerType) {
  var cb = {
    contents: (function (ev) {
        return eventHandlerGetCB(handlerType, ev);
      })
  };
  var handler = eventHandler(callbacks, cb);
  elem.addEventListener(name, handler);
  return {
          handler: handler,
          cb: cb
        };
}

function eventHandlerUnregister(elem, name, cache) {
  if (cache !== undefined) {
    elem.removeEventListener(name, cache.handler);
    return ;
  }
  
}

function eventHandlerMutate(callbacks, elem, oldName, newName, oldHandlerType, newHandlerType, oldCache, newCache) {
  var oldcache = oldCache.contents;
  if (oldcache === undefined) {
    newCache.contents = eventHandlerRegister(callbacks, elem, newName, newHandlerType);
    return ;
  }
  if (oldName === newName) {
    newCache.contents = oldCache.contents;
    if (compareEventHandlerTypes(oldHandlerType, newHandlerType)) {
      return ;
    }
    var cb = function (ev) {
      return eventHandlerGetCB(newHandlerType, ev);
    };
    oldcache.cb.contents = cb;
    return ;
  }
  oldCache.contents = eventHandlerUnregister(elem, oldName, oldCache.contents);
  newCache.contents = eventHandlerRegister(callbacks, elem, newName, newHandlerType);
}

function patchVNodesOnElemsPropertiesApplyAdd(callbacks, elem, _idx, x) {
  if (typeof x !== "object") {
    return ;
  }
  switch (x.TAG) {
    case "RawProp" :
        elem[x._0] = x._1;
        return ;
    case "Attribute" :
        elem.setAttributeNS(x._0, x._1, x._2);
        return ;
    case "Data" :
        console.log([
              "TODO:  Add Data Unhandled",
              x._0,
              x._1
            ]);
        return PervasivesU.failwith("TODO:  Add Data Unhandled");
    case "Event" :
        x._2.contents = eventHandlerRegister(callbacks, elem, x._0, x._1);
        return ;
    case "Style" :
        var elem$1 = Webapi__Dom__HtmlElement.ofElement(elem);
        if (elem$1 === undefined) {
          return PervasivesU.failwith("Expected htmlelement in patchVNodesOnElems_PropertiesApplyAdd");
        }
        var elemStyle = Caml_option.valFromOption(elem$1).style;
        return List.fold_left((function (param, param$1) {
                      elemStyle.setProperty(param$1[0], param$1[1]);
                    }), undefined, x._0);
    
  }
}

function patchVNodesOnElemsPropertiesApplyRemove(_callbacks, elem, _idx, x) {
  if (typeof x !== "object") {
    return ;
  }
  switch (x.TAG) {
    case "RawProp" :
        var key = x._0;
        elem[key] = undefined;
        return ;
    case "Attribute" :
        elem.removeAttributeNS(x._0, x._1);
        return ;
    case "Data" :
        console.log([
              "TODO:  Remove Data Unhandled",
              x._0,
              x._1
            ]);
        return PervasivesU.failwith("TODO:  Remove Data Unhandled");
    case "Event" :
        var cache = x._2;
        cache.contents = eventHandlerUnregister(elem, x._0, cache.contents);
        return ;
    case "Style" :
        var elem$1 = Webapi__Dom__HtmlElement.ofElement(elem);
        if (elem$1 === undefined) {
          return PervasivesU.failwith("Expected htmlelement in patchVNodesOnElems_PropertiesApply_Remove");
        }
        var elemStyle = Caml_option.valFromOption(elem$1).style;
        return List.fold_left((function (param, param$1) {
                      elemStyle.removeProperty(param$1[0]);
                    }), undefined, x._0);
    
  }
}

function patchVNodesOnElemsPropertiesApplyRemoveAdd(callbacks, elem, idx, oldProp, newProp) {
  patchVNodesOnElemsPropertiesApplyRemove(callbacks, elem, idx, oldProp);
  patchVNodesOnElemsPropertiesApplyAdd(callbacks, elem, idx, newProp);
}

function patchVNodesOnElemsPropertiesApplyMutate(_callbacks, elem, _idx, oldProp, x) {
  if (typeof x !== "object") {
    return PervasivesU.failwith("This should never be called as all entries through NoProp are gated.");
  }
  switch (x.TAG) {
    case "RawProp" :
        elem[x._0] = x._1;
        return ;
    case "Attribute" :
        elem.setAttributeNS(x._0, x._1, x._2);
        return ;
    case "Data" :
        console.log([
              "TODO:  Mutate Data Unhandled",
              x._0,
              x._1
            ]);
        return PervasivesU.failwith("TODO:  Mutate Data Unhandled");
    case "Event" :
        return PervasivesU.failwith("This will never be called because it is gated");
    case "Style" :
        var elem$1 = Webapi__Dom__HtmlElement.ofElement(elem);
        if (elem$1 === undefined) {
          return PervasivesU.failwith("Expected htmlelement in patchVNodesOnElemsPropertiesApplyMutate");
        }
        var elemStyle = Caml_option.valFromOption(elem$1).style;
        if (typeof oldProp !== "object" || oldProp.TAG !== "Style") {
          return PervasivesU.failwith("Passed a non-Style to a new Style as a Mutations while the old Style is not actually a style!");
        } else {
          return List.fold_left2((function (param, param$1, param$2) {
                        var nv = param$2[1];
                        var nk = param$2[0];
                        var ok = param$1[0];
                        if (ok === nk) {
                          if (param$1[1] === nv) {
                            return ;
                          } else {
                            elemStyle.setProperty(nk, nv);
                            return ;
                          }
                        } else {
                          elemStyle.removeProperty(ok);
                          elemStyle.setProperty(nk, nv);
                          return ;
                        }
                      }), undefined, oldProp._0, x._0);
        }
    
  }
}

function patchVNodesOnElemsPropertiesApply(callbacks, elem, _idx, _oldProperties, _newProperties) {
  while(true) {
    var newProperties = _newProperties;
    var oldProperties = _oldProperties;
    var idx = _idx;
    if (!oldProperties) {
      if (newProperties) {
        return false;
      } else {
        return true;
      }
    }
    var _oldProp = oldProperties.hd;
    if (!newProperties) {
      return false;
    }
    if (typeof _oldProp !== "object") {
      var tmp = newProperties.hd;
      if (typeof tmp !== "object") {
        _newProperties = newProperties.tl;
        _oldProperties = oldProperties.tl;
        _idx = idx + 1 | 0;
        continue ;
      }
      
    } else {
      switch (_oldProp.TAG) {
        case "RawProp" :
            var newProp = newProperties.hd;
            if (typeof newProp === "object" && newProp.TAG === "RawProp") {
              if (_oldProp._0 === newProp._0 && _oldProp._1 === newProp._1) {
                
              } else {
                patchVNodesOnElemsPropertiesApplyMutate(callbacks, elem, idx, _oldProp, newProp);
              }
              _newProperties = newProperties.tl;
              _oldProperties = oldProperties.tl;
              _idx = idx + 1 | 0;
              continue ;
            }
            break;
        case "Attribute" :
            var newProp$1 = newProperties.hd;
            if (typeof newProp$1 === "object" && newProp$1.TAG === "Attribute") {
              if (_oldProp._0 === newProp$1._0 && _oldProp._1 === newProp$1._1 && _oldProp._2 === newProp$1._2) {
                
              } else {
                patchVNodesOnElemsPropertiesApplyMutate(callbacks, elem, idx, _oldProp, newProp$1);
              }
              _newProperties = newProperties.tl;
              _oldProperties = oldProperties.tl;
              _idx = idx + 1 | 0;
              continue ;
            }
            break;
        case "Data" :
            var newProp$2 = newProperties.hd;
            if (typeof newProp$2 === "object" && newProp$2.TAG === "Data") {
              if (_oldProp._0 === newProp$2._0 && _oldProp._1 === newProp$2._1) {
                
              } else {
                patchVNodesOnElemsPropertiesApplyMutate(callbacks, elem, idx, _oldProp, newProp$2);
              }
              _newProperties = newProperties.tl;
              _oldProperties = oldProperties.tl;
              _idx = idx + 1 | 0;
              continue ;
            }
            break;
        case "Event" :
            var _newProp = newProperties.hd;
            if (typeof _newProp === "object" && _newProp.TAG === "Event") {
              eventHandlerMutate(callbacks, elem, _oldProp._0, _newProp._0, _oldProp._1, _newProp._1, _oldProp._2, _newProp._2);
              _newProperties = newProperties.tl;
              _oldProperties = oldProperties.tl;
              _idx = idx + 1 | 0;
              continue ;
            }
            break;
        case "Style" :
            var newProp$3 = newProperties.hd;
            if (typeof newProp$3 === "object" && newProp$3.TAG === "Style") {
              if (Caml_obj.equal(_oldProp._0, newProp$3._0)) {
                
              } else {
                patchVNodesOnElemsPropertiesApplyMutate(callbacks, elem, idx, _oldProp, newProp$3);
              }
              _newProperties = newProperties.tl;
              _oldProperties = oldProperties.tl;
              _idx = idx + 1 | 0;
              continue ;
            }
            break;
        
      }
    }
    patchVNodesOnElemsPropertiesApplyRemoveAdd(callbacks, elem, idx, _oldProp, newProperties.hd);
    _newProperties = newProperties.tl;
    _oldProperties = oldProperties.tl;
    _idx = idx + 1 | 0;
    continue ;
  };
}

function patchVNodesOnElemsProperties(callbacks, elem, oldProperties, newProperties) {
  return patchVNodesOnElemsPropertiesApply(callbacks, elem, 0, oldProperties, newProperties);
}

function genEmptyProps(length) {
  var _lst = /* [] */0;
  var _x = length;
  while(true) {
    var x = _x;
    var lst = _lst;
    if (x === 0) {
      return lst;
    }
    _x = x - 1 | 0;
    _lst = {
      hd: "NoProp",
      tl: lst
    };
    continue ;
  };
}

function mapEmptyProps(props) {
  return List.map((function (param) {
                return "NoProp";
              }), props);
}

function patchVNodesOnElemsReplaceNode(callbacks, elem, elems, idx, newNode) {
  if (newNode.TAG !== "Node") {
    return PervasivesU.failwith("Node replacement should never be passed anything but a node itself");
  }
  var newProperties = newNode._4;
  var oldChild = nodeAt(idx, elems);
  var newChild = createElementNsOptional(newNode._0, newNode._1);
  patchVNodesOnElemsProperties(callbacks, newChild, List.map((function (param) {
              return "NoProp";
            }), newProperties), newProperties);
  var childChildren = newChild.childNodes;
  patchVNodesOnElems(callbacks, newChild, childChildren, 0, /* [] */0, newNode._5);
  elem.insertBefore(newChild, oldChild);
  elem.removeChild(oldChild);
}

function patchVNodesOnElemsCreateElement(_callbacks, _newNode) {
  while(true) {
    var newNode = _newNode;
    var callbacks = _callbacks;
    switch (newNode.TAG) {
      case "CommentNode" :
          return document.createComment(newNode._0);
      case "Text" :
          return document.createTextNode(newNode._0);
      case "Node" :
          var newProperties = newNode._4;
          var newChild = createElementNsOptional(newNode._0, newNode._1);
          var match = patchVNodesOnElemsProperties(callbacks, newChild, List.map((function (param) {
                      return "NoProp";
                    }), newProperties), newProperties);
          if (match) {
            var childChildren = newChild.childNodes;
            patchVNodesOnElems(callbacks, newChild, childChildren, 0, /* [] */0, newNode._5);
            return newChild;
          }
          throw {
                RE_EXN_ID: "Match_failure",
                _1: [
                  "vdom.res",
                  482,
                  8
                ],
                Error: new Error()
              };
      case "LazyGen" :
          var vdom = newNode._1();
          newNode._2.contents = vdom;
          _newNode = vdom;
          continue ;
      case "Tagger" :
          _newNode = newNode._1;
          _callbacks = newNode._0(callbacks);
          continue ;
      
    }
  };
}

function patchVNodesOnElemsMutateNode(callbacks, elem, elems, idx, oldNode, newNode) {
  if (oldNode.TAG !== "Node") {
    return PervasivesU.failwith("Non-node passed to patchVNodesOnElemsMutateNode");
  }
  if (newNode.TAG !== "Node") {
    return PervasivesU.failwith("Non-node passed to patchVNodesOnElemsMutateNode");
  }
  if (oldNode._3 !== newNode._3 || oldNode._1 !== newNode._1) {
    return patchVNodesOnElemsReplaceNode(callbacks, elem, elems, idx, newNode);
  }
  var child = nodeAt(idx, elems);
  var childElement = Webapi__Dom__Element.ofNode(child);
  if (childElement === undefined) {
    return PervasivesU.failwith("Expected element in patchVNodesOnElems_MutateNode");
  }
  var childChildren = child.childNodes;
  if (patchVNodesOnElemsProperties(callbacks, Caml_option.valFromOption(childElement), oldNode._4, newNode._4)) {
    
  } else {
    console.log("VDom:  Failed swapping properties because the property list length changed, use `noProp` to swap properties instead, not by altering the list structure.  This is a massive inefficiency until this issue is resolved.");
    patchVNodesOnElemsReplaceNode(callbacks, elem, elems, idx, newNode);
  }
  patchVNodesOnElems(callbacks, child, childChildren, 0, oldNode._5, newNode._5);
}

function patchVNodesOnElems(callbacks, elem, elems, _idx, _oldVNodes, _newVNodes) {
  while(true) {
    var newVNodes = _newVNodes;
    var oldVNodes = _oldVNodes;
    var idx = _idx;
    if (oldVNodes) {
      var oldNode = oldVNodes.hd;
      switch (oldNode.TAG) {
        case "CommentNode" :
            if (newVNodes) {
              var newS = newVNodes.hd;
              if (newS.TAG === "CommentNode" && oldNode._0 === newS._0) {
                _newVNodes = newVNodes.tl;
                _oldVNodes = oldVNodes.tl;
                _idx = idx + 1 | 0;
                continue ;
              }
              
            }
            break;
        case "Text" :
            if (newVNodes) {
              var newText = newVNodes.hd;
              if (newText.TAG === "Text") {
                var newText$1 = newText._0;
                if (oldNode._0 !== newText$1) {
                  var child = nodeAt(idx, elems);
                  child.nodeValue = newText$1;
                }
                _newVNodes = newVNodes.tl;
                _oldVNodes = oldVNodes.tl;
                _idx = idx + 1 | 0;
                continue ;
              }
              
            }
            break;
        case "Node" :
            if (newVNodes) {
              var newNode = newVNodes.hd;
              if (newNode.TAG === "Node") {
                var newRest = newVNodes.tl;
                var newKey = newNode._2;
                var newTagName = newNode._1;
                var newNamespace = newNode._0;
                var oldRest = oldVNodes.tl;
                var oldKey = oldNode._2;
                var oldTagName = oldNode._1;
                var oldNamespace = oldNode._0;
                if (oldKey === newKey && oldKey !== "") {
                  _newVNodes = newRest;
                  _oldVNodes = oldRest;
                  _idx = idx + 1 | 0;
                  continue ;
                }
                if (oldKey === "" || newKey === "") {
                  patchVNodesOnElemsMutateNode(callbacks, elem, elems, idx, oldNode, newNode);
                  _newVNodes = newRest;
                  _oldVNodes = oldRest;
                  _idx = idx + 1 | 0;
                  continue ;
                }
                var exit = 0;
                var exit$1 = 0;
                if (oldRest) {
                  var match = oldRest.hd;
                  if (match.TAG === "Node") {
                    var olderRest = oldRest.tl;
                    var olderKey = match._2;
                    var olderTagName = match._1;
                    var olderNamespace = match._0;
                    var exit$2 = 0;
                    if (newRest) {
                      var match$1 = newRest.hd;
                      if (match$1.TAG === "Node") {
                        if (olderNamespace === newNamespace && olderTagName === newTagName && olderKey === newKey && oldNamespace === match$1._0 && oldTagName === match$1._1 && oldKey === match$1._2) {
                          var firstChild = nodeAt(idx, elems);
                          var secondChild = nodeAt(idx + 1 | 0, elems);
                          elem.removeChild(secondChild);
                          elem.insertBefore(secondChild, firstChild);
                          _newVNodes = newRest.tl;
                          _oldVNodes = olderRest;
                          _idx = idx + 2 | 0;
                          continue ;
                        }
                        exit$2 = 4;
                      } else {
                        exit$2 = 4;
                      }
                    } else {
                      exit$2 = 4;
                    }
                    if (exit$2 === 4) {
                      if (olderNamespace === newNamespace && olderTagName === newTagName && olderKey === newKey) {
                        var oldChild = nodeAt(idx, elems);
                        elem.removeChild(oldChild);
                        _newVNodes = newRest;
                        _oldVNodes = olderRest;
                        _idx = idx + 1 | 0;
                        continue ;
                      }
                      exit$1 = 3;
                    }
                    
                  } else {
                    exit$1 = 3;
                  }
                } else {
                  exit$1 = 3;
                }
                if (exit$1 === 3) {
                  if (newRest) {
                    var match$2 = newRest.hd;
                    if (match$2.TAG === "Node") {
                      if (oldNamespace === match$2._0 && oldTagName === match$2._1 && oldKey === match$2._2) {
                        var oldChild$1 = nodeAt(idx, elems);
                        var newChild = patchVNodesOnElemsCreateElement(callbacks, newNode);
                        elem.insertBefore(newChild, oldChild$1);
                        _newVNodes = newRest;
                        _idx = idx + 1 | 0;
                        continue ;
                      }
                      exit = 2;
                    } else {
                      exit = 2;
                    }
                  } else {
                    exit = 2;
                  }
                }
                if (exit === 2) {
                  patchVNodesOnElemsMutateNode(callbacks, elem, elems, idx, oldNode, newNode);
                  _newVNodes = newRest;
                  _oldVNodes = oldRest;
                  _idx = idx + 1 | 0;
                  continue ;
                }
                
              }
              
            }
            break;
        case "LazyGen" :
            if (newVNodes) {
              var match$3 = newVNodes.hd;
              if (match$3.TAG === "LazyGen") {
                var newRest$1 = newVNodes.tl;
                var newCache = match$3._2;
                var newGen = match$3._1;
                var newKey$1 = match$3._0;
                var oldRest$1 = oldVNodes.tl;
                var oldCache = oldNode._2;
                var oldKey$1 = oldNode._0;
                if (oldKey$1 === newKey$1) {
                  newCache.contents = oldCache.contents;
                  _newVNodes = newRest$1;
                  _oldVNodes = oldRest$1;
                  _idx = idx + 1 | 0;
                  continue ;
                }
                var exit$3 = 0;
                var exit$4 = 0;
                if (oldRest$1) {
                  var match$4 = oldRest$1.hd;
                  if (match$4.TAG === "LazyGen") {
                    var olderRest$1 = oldRest$1.tl;
                    var olderKey$1 = match$4._0;
                    var exit$5 = 0;
                    if (newRest$1) {
                      var match$5 = newRest$1.hd;
                      if (match$5.TAG === "LazyGen") {
                        if (olderKey$1 === newKey$1 && oldKey$1 === match$5._0) {
                          var firstChild$1 = nodeAt(idx, elems);
                          var secondChild$1 = nodeAt(idx + 1 | 0, elems);
                          elem.removeChild(secondChild$1);
                          elem.insertBefore(secondChild$1, firstChild$1);
                          _newVNodes = newRest$1.tl;
                          _oldVNodes = olderRest$1;
                          _idx = idx + 2 | 0;
                          continue ;
                        }
                        exit$5 = 4;
                      } else {
                        exit$5 = 4;
                      }
                    } else {
                      exit$5 = 4;
                    }
                    if (exit$5 === 4) {
                      if (olderKey$1 === newKey$1) {
                        var oldChild$2 = nodeAt(idx, elems);
                        elem.removeChild(oldChild$2);
                        var oldVdom = match$4._2.contents;
                        newCache.contents = oldVdom;
                        _newVNodes = newRest$1;
                        _oldVNodes = olderRest$1;
                        _idx = idx + 1 | 0;
                        continue ;
                      }
                      exit$4 = 3;
                    }
                    
                  } else {
                    exit$4 = 3;
                  }
                } else {
                  exit$4 = 3;
                }
                if (exit$4 === 3) {
                  if (newRest$1) {
                    var match$6 = newRest$1.hd;
                    if (match$6.TAG === "LazyGen") {
                      if (match$6._0 === oldKey$1) {
                        var oldChild$3 = nodeAt(idx, elems);
                        var newVdom = newGen();
                        newCache.contents = newVdom;
                        var newChild$1 = patchVNodesOnElemsCreateElement(callbacks, newVdom);
                        elem.insertBefore(newChild$1, oldChild$3);
                        _newVNodes = newRest$1;
                        _idx = idx + 1 | 0;
                        continue ;
                      }
                      exit$3 = 2;
                    } else {
                      exit$3 = 2;
                    }
                  } else {
                    exit$3 = 2;
                  }
                }
                if (exit$3 === 2) {
                  var oldVdom$1 = oldCache.contents;
                  var newVdom$1 = newGen();
                  newCache.contents = newVdom$1;
                  _newVNodes = {
                    hd: newVdom$1,
                    tl: newRest$1
                  };
                  _oldVNodes = {
                    hd: oldVdom$1,
                    tl: oldRest$1
                  };
                  continue ;
                }
                
              }
              
            }
            break;
        case "Tagger" :
            _oldVNodes = {
              hd: oldNode._1,
              tl: oldVNodes.tl
            };
            continue ;
        
      }
      var oldRest$2 = oldVNodes.tl;
      if (newVNodes) {
        var newNode$1 = newVNodes.hd;
        if (newNode$1.TAG === "Tagger") {
          patchVNodesOnElems(newNode$1._0(callbacks), elem, elems, idx, {
                hd: oldNode,
                tl: /* [] */0
              }, {
                hd: newNode$1._1,
                tl: /* [] */0
              });
          _newVNodes = newVNodes.tl;
          _oldVNodes = oldRest$2;
          _idx = idx + 1 | 0;
          continue ;
        }
        var oldChild$4 = nodeAt(idx, elems);
        var newChild$2 = patchVNodesOnElemsCreateElement(callbacks, newNode$1);
        elem.insertBefore(newChild$2, oldChild$4);
        elem.removeChild(oldChild$4);
        _newVNodes = newVNodes.tl;
        _oldVNodes = oldRest$2;
        _idx = idx + 1 | 0;
        continue ;
      }
      var child$1 = nodeAt(idx, elems);
      elem.removeChild(child$1);
      _newVNodes = /* [] */0;
      _oldVNodes = oldRest$2;
      continue ;
    }
    if (!newVNodes) {
      return ;
    }
    var newChild$3 = patchVNodesOnElemsCreateElement(callbacks, newVNodes.hd);
    elem.appendChild(newChild$3);
    _newVNodes = newVNodes.tl;
    _oldVNodes = /* [] */0;
    _idx = idx + 1 | 0;
    continue ;
  };
}

function patchVNodesIntoElement(callbacks, elem, oldVNodes, newVNodes) {
  var elems = elem.childNodes;
  patchVNodesOnElems(callbacks, elem, elems, 0, oldVNodes, newVNodes);
  return newVNodes;
}

function patchVNodeIntoElement(callbacks, elem, oldVNode, newVNode) {
  return patchVNodesIntoElement(callbacks, elem, {
              hd: oldVNode,
              tl: /* [] */0
            }, {
              hd: newVNode,
              tl: /* [] */0
            });
}

function wrapCallbacksOn(func, x) {
  if (typeof x !== "object") {
    return "Render";
  } else if (x.TAG === "AddRenderMsg") {
    return {
            TAG: "AddRenderMsg",
            _0: func(x._0)
          };
  } else {
    return {
            TAG: "RemoveRenderMsg",
            _0: func(x._0)
          };
  }
}

function wrapCallbacks(func, callbacks) {
  return {
          contents: {
            enqueue: (function (msg) {
                var new_msg = func(msg);
                callbacks.contents.enqueue(new_msg);
              }),
            on: (function (smsg) {
                var new_smsg = wrapCallbacksOn(func, smsg);
                callbacks.contents.on(new_smsg);
              })
          }
        };
}

function map(func, vdom) {
  var tagger = function (callbacks) {
    return wrapCallbacks(func, callbacks);
  };
  return {
          TAG: "Tagger",
          _0: tagger,
          _1: vdom
        };
}

var noProp = "NoProp";

export {
  noNode ,
  comment ,
  text ,
  fullnode ,
  node ,
  lazyGen ,
  noProp ,
  prop ,
  onCB ,
  onMsg ,
  attribute ,
  data ,
  style ,
  styles ,
  renderToHtmlString ,
  emptyEventHandler ,
  emptyEventCB ,
  eventHandler ,
  eventHandlerGetCB ,
  compareEventHandlerTypes ,
  eventHandlerRegister ,
  eventHandlerUnregister ,
  eventHandlerMutate ,
  patchVNodesOnElemsPropertiesApplyAdd ,
  patchVNodesOnElemsPropertiesApplyRemove ,
  patchVNodesOnElemsPropertiesApplyRemoveAdd ,
  patchVNodesOnElemsPropertiesApplyMutate ,
  patchVNodesOnElemsPropertiesApply ,
  patchVNodesOnElemsProperties ,
  genEmptyProps ,
  mapEmptyProps ,
  patchVNodesOnElemsReplaceNode ,
  patchVNodesOnElemsCreateElement ,
  patchVNodesOnElemsMutateNode ,
  patchVNodesOnElems ,
  patchVNodesIntoElement ,
  patchVNodeIntoElement ,
  wrapCallbacksOn ,
  wrapCallbacks ,
  map ,
}
/* Webapi__Dom__Element Not a pure module */
