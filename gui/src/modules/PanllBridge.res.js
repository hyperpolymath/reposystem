// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";

function isPanllHost() {
  return Belt_Option.isSome(Caml_option.nullable_to_opt(window.__PANLL_INTERNALS__));
}

function connectionLabel(status) {
  if (typeof status !== "object") {
    if (status === "PanllDisconnected") {
      return "Disconnected";
    } else {
      return "Connecting...";
    }
  } else if (status.TAG === "PanllConnected") {
    return "Connected (" + status._0 + ")";
  } else {
    return "Error: " + status._0;
  }
}

function isConnected(state) {
  var match = state.connection;
  if (typeof match !== "object" || match.TAG !== "PanllConnected") {
    return false;
  } else {
    return true;
  }
}

var init = {
  connection: "PanllDisconnected",
  lastSentAt: undefined,
  lastReceivedAt: undefined,
  autoSync: false,
  instanceId: undefined
};

var defaultEndpoint = "http://localhost:1430";

export {
  init ,
  defaultEndpoint ,
  isPanllHost ,
  connectionLabel ,
  isConnected ,
}
/* No side effect */
