// Generated by ReScript, PLEASE EDIT WITH CARE

import * as List from "rescript/lib/es6/list.js";
import * as $$Array from "rescript/lib/es6/array.js";
import * as Js_string from "rescript/lib/es6/js_string.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as PervasivesU from "rescript/lib/es6/pervasivesU.js";
import * as Belt_MapString from "rescript/lib/es6/belt_MapString.js";

function abort(x) {
  x.abort();
}

function getAllResponseHeaders(x) {
  var s = x.getAllResponseHeaders();
  if (s !== null) {
    if (s === "") {
      return {
              TAG: "Error",
              _0: "NetworkError"
            };
    } else {
      return {
              TAG: "Ok",
              _0: s
            };
    }
  } else {
    return {
            TAG: "Error",
            _0: "IncompleteResponse"
          };
  }
}

function getAllResponseHeadersAsList(x) {
  var err = getAllResponseHeaders(x);
  if (err.TAG !== "Ok") {
    return err;
  }
  var lines = Js_string.split("\r\n", err._0);
  var parts = $$Array.to_list($$Array.map((function (line) {
              return Js_string.splitAtMost(": ", 2, line);
            }), lines));
  var filtered = List.filter(function (a) {
          return a.length === 2;
        })(parts);
  return {
          TAG: "Ok",
          _0: List.map((function (x) {
                  if (x.length !== 2) {
                    return PervasivesU.failwith("Cannot happen, already checked length");
                  }
                  var key = x[0];
                  var value = x[1];
                  return [
                          key,
                          value
                        ];
                }), filtered)
        };
}

function getAllResponseHeadersAsDict(x) {
  var err = getAllResponseHeadersAsList(x);
  if (err.TAG !== "Ok") {
    return err;
  }
  var insert = function (d, param) {
    return Belt_MapString.set(d, param[0], param[1]);
  };
  return {
          TAG: "Ok",
          _0: List.fold_left(insert, undefined, err._0)
        };
}

function open$p(method$p, url, asyncOpt, userOpt, passwordOpt, x) {
  var async = asyncOpt !== undefined ? asyncOpt : true;
  var user = userOpt !== undefined ? userOpt : "";
  var password = passwordOpt !== undefined ? passwordOpt : "";
  x.open(method$p, url, async, user, password);
}

function overrideMimeType(mimetype, x) {
  x.overrideMimeType(mimetype);
}

function send(body, x) {
  if (typeof body !== "object") {
    if (body === "EmptyBody") {
      x.send();
      return ;
    }
    x.send(null);
    return ;
  } else {
    switch (body.TAG) {
      case "FormListBody" :
          var form = List.fold_left((function (f, param) {
                  f.append(param[0], param[1]);
                  return f;
                }), new FormData(), body._0);
          x.send(form);
          return ;
      case "StringBody" :
      case "FormDataBody" :
      case "DocumentBody" :
          x.send(body._0);
          return ;
      
    }
  }
}

function setRequestHeader(header, value, x) {
  x.setRequestHeader(header, value);
}

function set_onreadystatechange(cb, x) {
  x.onreadystatechange = cb;
}

function get_onreadystatechange(x) {
  return x.onreadystatechange;
}

function readyState(x) {
  var i = x.readyState;
  switch (i) {
    case 0 :
        return "Unsent";
    case 1 :
        return "Opened";
    case 2 :
        return "HeadersReceived";
    case 3 :
        return "Loading";
    case 4 :
        return "Done";
    default:
      return PervasivesU.failwith("Invalid return from 'readystate' of: " + String(i));
  }
}

function set_responseType(typ, x) {
  if (typeof typ === "object") {
    x.responseType = typ._0;
    return ;
  }
  switch (typ) {
    case "StringResponseType" :
        x.responseType = "";
        return ;
    case "ArrayBufferResponseType" :
        x.responseType = "arraybuffer";
        return ;
    case "BlobResponseType" :
        x.responseType = "blob";
        return ;
    case "DocumentResponseType" :
        x.responseType = "document";
        return ;
    case "JsonResponseType" :
        x.responseType = "json";
        return ;
    case "TextResponseType" :
        x.responseType = "text";
        return ;
    
  }
}

function get_responseType(x) {
  var s = x.responseType;
  switch (s) {
    case "" :
        return "StringResponseType";
    case "arraybuffer" :
        return "ArrayBufferResponseType";
    case "blob" :
        return "BlobResponseType";
    case "document" :
        return "DocumentResponseType";
    case "json" :
        return "JsonResponseType";
    case "text" :
        return "TextResponseType";
    default:
      return {
              TAG: "RawResponseType",
              _0: s
            };
  }
}

function get_response(x) {
  var resp = x.response;
  if (resp === null) {
    return "NoResponse";
  }
  var s = get_responseType(x);
  if (typeof s === "object") {
    return {
            TAG: "RawResponse",
            _0: s._0,
            _1: resp
          };
  }
  switch (s) {
    case "StringResponseType" :
        return {
                TAG: "StringResponse",
                _0: resp
              };
    case "ArrayBufferResponseType" :
        return {
                TAG: "ArrayBufferResponse",
                _0: resp
              };
    case "BlobResponseType" :
        return {
                TAG: "BlobResponse",
                _0: resp
              };
    case "DocumentResponseType" :
        return {
                TAG: "DocumentResponse",
                _0: resp
              };
    case "JsonResponseType" :
        return {
                TAG: "JsonResponse",
                _0: resp
              };
    case "TextResponseType" :
        return {
                TAG: "TextResponse",
                _0: resp
              };
    
  }
}

function get_responseText(x) {
  return x.responseText;
}

function get_responseURL(x) {
  return x.responseURL;
}

function get_responseXML(x) {
  return Caml_option.null_to_opt(x.responseXML);
}

function get_status(x) {
  return x.status;
}

function get_statusText(x) {
  return x.statusText;
}

function set_timeout(t, x) {
  x.timeout = t;
}

function get_timeout(x) {
  return x.timeout;
}

function set_withCredentials(b, x) {
  x.withCredentials = b;
}

function get_withCredentials(x) {
  return x.withCredentials;
}

function set_onabort(cb, x) {
  x.onabort = cb;
}

function get_onabort(x) {
  return x.onabort;
}

function set_onerror(cb, x) {
  x.onerror = cb;
}

function get_onerror(x) {
  return x.onerror;
}

function set_onload(cb, x) {
  x.onload = cb;
}

function get_onload(x) {
  return x.onload;
}

function set_onloadstart(cb, x) {
  x.onloadstart = cb;
}

function get_onloadstart(x) {
  return x.onloadstart;
}

function set_onprogress(cb, x) {
  x.onprogress = cb;
}

function get_onprogress(x) {
  return x.onprogress;
}

function set_ontimeout(cb, x) {
  x.ontimeout = cb;
}

function get_ontimeout(x) {
  return x.ontimeout;
}

function set_onloadend(cb, x) {
  x.onloadend = cb;
}

function get_onloadend(x) {
  return x.onloadend;
}

export {
  abort ,
  getAllResponseHeaders ,
  getAllResponseHeadersAsList ,
  getAllResponseHeadersAsDict ,
  open$p ,
  overrideMimeType ,
  send ,
  setRequestHeader ,
  set_onreadystatechange ,
  get_onreadystatechange ,
  readyState ,
  set_responseType ,
  get_responseType ,
  get_response ,
  get_responseText ,
  get_responseURL ,
  get_responseXML ,
  get_status ,
  get_statusText ,
  set_timeout ,
  get_timeout ,
  set_withCredentials ,
  get_withCredentials ,
  set_onabort ,
  get_onabort ,
  set_onerror ,
  get_onerror ,
  set_onload ,
  get_onload ,
  set_onloadstart ,
  get_onloadstart ,
  set_onprogress ,
  get_onprogress ,
  set_ontimeout ,
  get_ontimeout ,
  set_onloadend ,
  get_onloadend ,
}
/* No side effect */
